# Build TODOs — Image Eval Pipeline

Generated by `/plan-eng-review` on 2026-04-25

## Phase 0: Setup (before writing code)
- [ ] Set up Google AI / Vertex AI access for Gemini 3 Pro image generation (model: `gemini-3-pro-image-preview`)
- [ ] Verify Anthropic multimodal image input (Claude Opus 4) — max image size, token cost
- [ ] Verify OpenAI GPT-5.4 multimodal image input — max image size, token cost
- [ ] Set up env vars: `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `GOOGLE_API_KEY`

## Phase 1: Headless Pipeline (hours 1-4)
- [ ] `uv init` + install deps: `openai`, `anthropic`, `google-genai`, `pydantic`, `streamlit`, `plotly`, `python-dotenv`, `pytest`
- [ ] `schemas.py`: Pydantic models — `ImageEvaluation`, `CritiqueResponse`, `RevisedEvaluation`, `ComparisonResult`
- [ ] `generate.py`: ThreadPoolExecutor parallel image generation (GPT Image-2 + Gemini 3 Pro) → save PNGs to `runs/`
- [ ] `evaluate.py`: Claude Opus scores both images via base64 content blocks, anchored rubric in system prompt → structured JSON
- [ ] `critique.py`: GPT-5.4 reviews evaluation + both images → dimension-level feedback with suggested adjustments
- [ ] `revise.py`: Claude Opus full re-evaluation (all 6 dimensions) with critique context + both images → final structured output
- [ ] `compare.py`: Winner determination (mean score, tiebreak by largest single-dimension lead)
- [ ] `pipeline.py`: Orchestrator wiring all steps together, persists all intermediate JSON to `runs/`
- [ ] End-to-end test with 3 hardcoded prompts

## Phase 2: Streamlit Dashboard (hours 5-8)
- [ ] `app.py`: Streamlit app shell with dark theme config
- [ ] Pre-baked results dropdown: "Load example: [...]" above prompt bar
- [ ] Prompt input bar + "Generate & Evaluate" button
- [ ] `st.session_state` for all pipeline results
- [ ] Stacked results: `st.session_state` stores list of run objects, most recent on top, horizontal dividers between runs, each labeled with prompt text and timestamp (design review DD3)
- [ ] `st.status()` container for real-time pipeline progress
- [ ] Implement interaction states: loading stages per pipeline step, error state visuals, empty/first-time state, partial success handling per Interaction States section in design doc (design review DD2)
- [ ] Side-by-side image display with model labels
- [ ] Winner banner between images and radar chart: "Winner: [Model Name] (X/6 dimensions) | Overall: X.X vs X.X" (design review DD1)
- [ ] Plotly radar chart with before/after overlay (~30-50 lines frame config) — animation spec: 800ms ease-in-out, center-out transition (design review DD7)
- [ ] Per-dimension score cards with delta annotations
- [ ] Critique transcript view (3-step pipeline)
- [ ] Raw JSON expander per evaluation step

## Phase 3: Demo Polish (hours 9-10)
- [ ] Pre-generate 5-8 compelling prompt results and cache to `runs/`
- [ ] Dark theme styling (matches wireframe)
- [ ] Before/after radar chart frame animation
- [ ] Winner badge with dimension count

## Tests (8 total)
- [ ] `test_schemas.py` (6): Schema validation for all Pydantic models
- [ ] `test_winner.py` (2): Winner determination logic + tiebreaking

## NOT in scope
- Deployment infrastructure
- User accounts / auth
- Multi-round debate (>1 revision)
- DALL-E 3 fallback generator
- Confidence scores
- Ground truth / statistical reliability
- Image resizing / normalization
- Integration tests (deferred)
- Cache loading tests (deferred)
